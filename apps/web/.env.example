# Environment variables for kali-personal-assistant
# Copy this file to .env and fill in the values.
# Values marked (required) must be provided for the app to run.
# Optional values enable extra functionality.

# Runtime environment: development | production | test (optional; default=development)
NODE_ENV=development

# Turso / libSQL database (required)
TURSO_DATABASE_URL="libsql://<your-database-name>.turso.io"
TURSO_AUTH_TOKEN="<turso-auth-token>"

# Better Auth core secrets (required)
# Generate a strong random string (e.g. 32+ chars) for the auth secret.
BETTER_AUTH_SECRET="<random-secret>"

# Public base URL of your app (required for email links)
# Example for local dev: http://localhost:3000
BETTER_AUTH_BASE_URL="http://localhost:3000"

# Resend Email (optional) — enable to actually send verification & reset emails.
# If omitted, emails will be logged to the server console instead.
RESEND_API_KEY="re_<your-resend-api-key>"
RESEND_FROM_EMAIL="Your App <no-reply@your-domain.com>"

# ============================
# HashiCorp Vault (Secrets Management)
# ============================
# These are generated by running: ./scripts/setup-vault.sh
# See .vault-credentials.env for the generated values.

# Vault server address (localhost for local dev, vault-dev:8200 for Docker)
VAULT_ADDR="http://127.0.0.1:8200"

# AppRole authentication credentials (from setup-vault.sh output)
VAULT_ROLE_ID="<nuxt-role-id>"
VAULT_SECRET_ID="<nuxt-secret-id>"

# (Optional examples you might add later)
# API_BASE_URL="https://api.example.com"

# --- Guidance ---
# 1. After setting these, restart the dev server so env changes are picked up.
# 2. NEVER commit your real .env; keep only .env.example in git.
# 3. For production, ensure BETTER_AUTH_BASE_URL matches the deployed HTTPS URL.
# 4. Rotate BETTER_AUTH_SECRET if compromised (all sessions invalidate).
# 5. Resend warm-up: Use a verified domain or sender to improve deliverability.
