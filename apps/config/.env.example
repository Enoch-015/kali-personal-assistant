# ============================
# Python Services Configuration
# ============================
# Copy this file to .env in the apps/config directory.
# These values are shared between AI and Voice services.

# ============================
# Vault Configuration
# ============================
# Default to localhost for local development.
# Use http://vault-dev:8200 if running services in Docker containers.
VAULT_ADDR="http://127.0.0.1:8200"

# ============================
# Service-Specific Credentials
# ============================
# Each service uses different AppRole credentials for least-privilege access.
# Copy from .vault-credentials.env after running ./scripts/setup-vault.sh

# For AI service (apps/ai)
# Set these in apps/ai/.env
# VAULT_ROLE_ID="<python-ai-role-id>"
# VAULT_SECRET_ID="<python-ai-secret-id>"

# For Voice service (apps/voice)
# Set these in apps/voice/.env
# VAULT_ROLE_ID="<livekit-role-id>"
# VAULT_SECRET_ID="<livekit-secret-id>"

# ============================
# Other Service URLs
# ============================
REDIS_URL="redis://localhost:6379/0"
NEO4J_URI="bolt://localhost:7687"
NEO4J_USERNAME="neo4j"
NEO4J_PASSWORD="password"
MONGODB_URI="mongodb://kali:kalipassword@localhost:27017"
